<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 基础应用主题 -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryDark">@color/primary_blue</item>
        <item name="colorAccent">@color/primary_blue</item>
        <item name="android:windowBackground">@color/bg_page</item>
        <item name="android:statusBarColor">@color/primary_blue</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <!-- 卡片样式 -->
    <style name="CardStyle">
        <item name="android:background">@drawable/bg_card</item>
        <item name="android:elevation">2dp</item>
        <item name="android:padding">@dimen/padding_card</item>
        <item name="cardCornerRadius">@dimen/radius_card</item>
        <item name="cardPreventCornerOverlap">true</item>
        <item name="cardUseCompatPadding">true</item>
    </style>

    <!-- 主按钮（蓝色填充） -->
    <style name="ButtonPrimary" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/bg_button_primary</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">@dimen/text_body</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingStart">@dimen/padding_button_h</item>
        <item name="android:paddingEnd">@dimen/padding_button_h</item>
        <item name="android:paddingTop">@dimen/padding_button_v</item>
        <item name="android:paddingBottom">@dimen/padding_button_v</item>
        <item name="android:minHeight">@dimen/height_button_normal</item>
        <item name="android:gravity">center</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- 次要按钮（白底蓝边） -->
    <style name="ButtonSecondary">
        <item name="android:background">@drawable/bg_button_secondary</item>
        <item name="android:textColor">@color/primary_blue</item>
        <item name="android:textSize">@dimen/text_body</item>
        <item name="android:paddingStart">@dimen/padding_button_h</item>
        <item name="android:paddingEnd">@dimen/padding_button_h</item>
        <item name="android:paddingTop">@dimen/padding_button_v</item>
        <item name="android:paddingBottom">@dimen/padding_button_v</item>
        <item name="android:minHeight">@dimen/height_button_normal</item>
        <item name="android:gravity">center</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <!-- 文字层级 -->
    <style name="TextLarge">
        <item name="android:textSize">@dimen/text_large</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextTitle">
        <item name="android:textSize">@dimen/text_title</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextBody">
        <item name="android:textSize">@dimen/text_body</item>
        <item name="android:textColor">@color/text_secondary</item>
    </style>

    <style name="TextSmall">
        <item name="android:textSize">@dimen/text_small</item>
        <item name="android:textColor">@color/text_hint</item>
    </style>
</resources>